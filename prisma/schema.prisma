datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

enum Role {
  SUPER_ADMIN
  MANDAL_ADMIN
  PRESIDENT
  TREASURER
  SECRETARY
  COLLECTOR
  VOLUNTEER
  VIEWER
}

enum PaymentMethod {
  CASH
  BANK
  UPI
  OTHER
}

enum TransactionType {
  INCOME
  EXPENSE
  TRANSFER
}

enum ApprovalStatus {
  DRAFT
  PENDING
  APPROVED
  REJECTED
  PAID
}

enum Language {
  mr // Marathi
  hi // Hindi
  en // English
}

model User {
  id               String          @id @default(uuid())
  email            String          @unique
  name             String
  phone            String?
  image            String?
  preferredLang    Language        @default(mr)
  createdAt        DateTime        @default(now())
  updatedAt        DateTime        @updatedAt

  memberships      MandalMember[]
  createdIncomes   Income[]        @relation("IncomeCreatedBy")
  createdExpenses  Expense[]       @relation("ExpenseCreatedBy")
  auditLogs        AuditLog[]
  approvals        Approval[]
  assignedAreas    AreaCollector[]

  @@index([email])
}

model Mandal {
  id              String            @id @default(uuid())
  name            String
  shortName       String
  joinCode        String            @unique // 6-digit code e.g. 482916
  address         String
  city            String
  district        String
  state           String            @default("Maharashtra")
  pincode         String
  contactNumber   String
  email           String?
  logoUrl         String?
  defaultCurrency String            @default("INR")
  defaultLang     Language          @default(mr)
  isSuspended     Boolean           @default(false)
  freeIncomeLimit Decimal           @default(50000.00) @db.Decimal(12, 2)
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt

  members         MandalMember[]
  festivals       Festival[]
  incomeHeads     IncomeCategory[]
  expenseHeads    ExpenseCategory[]
  accounts        Account[]
  donors          Donor[]
  receiptTemplate ReceiptTemplate?
  areas           Area[]
  auditLogs       AuditLog[]
  joinRequests    JoinRequest[]

  @@index([joinCode])
}

model MandalMember {
  id         String    @id @default(uuid())
  mandalId   String
  userId     String
  role       Role      @default(VOLUNTEER)
  isActive   Boolean   @default(true)
  joinedAt   DateTime  @default(now())

  mandal     Mandal    @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  user       User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([mandalId, userId])
  @@index([mandalId, role])
}

model JoinRequest {
  id         String         @id @default(uuid())
  mandalId   String
  userId     String
  status     ApprovalStatus @default(PENDING)
  notes      String?
  createdAt  DateTime       @default(now())

  mandal     Mandal         @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  
  @@unique([mandalId, userId])
}

model Festival {
  id            String         @id @default(uuid())
  mandalId      String
  name          String         // e.g. Ganeshotsav 2026
  financialYear String         // 2026-2027
  startDate     DateTime
  endDate       DateTime
  budget        Decimal        @default(0.00) @db.Decimal(12, 2)
  isActive      Boolean        @default(true)
  isArchived    Boolean        @default(false)
  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt

  mandal        Mandal         @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  transactions  Transaction[]
  incomes       Income[]
  expenses      Expense[]
  receipts      Receipt[]

  @@index([mandalId, isActive])
}

model Account {
  id             String          @id @default(uuid())
  mandalId       String
  name           String          // Cash in Hand, SBI Main A/c, Primary UPI
  type           PaymentMethod   // CASH, BANK, UPI
  accountNumber  String?
  ifscCode       String?
  openingBalance Decimal         @default(0.00) @db.Decimal(12, 2)
  currentBalance Decimal         @default(0.00) @db.Decimal(12, 2)
  isActive       Boolean         @default(true)

  mandal         Mandal          @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  ledgerEntries  LedgerEntry[]

  @@index([mandalId, type])
}

model IncomeCategory {
  id          String    @id @default(uuid())
  mandalId    String
  name        String    // Vargani, Donation, Sponsorship, Advertisement
  description String?
  isActive    Boolean   @default(true)
  sortOrder   Int       @default(0)

  mandal      Mandal    @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  incomes     Income[]

  @@index([mandalId])
}

model ExpenseCategory {
  id          String    @id @default(uuid())
  mandalId    String
  name        String    // Decoration, Mandap, Sound, Lighting, Prasad
  description String?
  isActive    Boolean   @default(true)
  sortOrder   Int       @default(0)

  mandal      Mandal    @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  expenses    Expense[]

  @@index([mandalId])
}

model Donor {
  id             String    @id @default(uuid())
  mandalId       String
  name           String
  phone          String?
  address        String?
  area           String?
  totalDonated   Decimal   @default(0.00) @db.Decimal(12, 2)
  donationCount  Int       @default(0)
  lastDonationAt DateTime?
  createdAt      DateTime  @default(now())

  mandal         Mandal    @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  incomes        Income[]

  @@index([mandalId, phone])
  @@index([mandalId, name])
}

model Transaction {
  id             String          @id @default(uuid())
  mandalId       String
  festivalId     String
  type           TransactionType
  amount         Decimal         @db.Decimal(12, 2)
  date           DateTime        @default(now())
  isArchived     Boolean         @default(false)
  archiveReason  String?
  idempotencyKey String?         @unique // For Offline Sync Deduplication
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt

  festival       Festival        @relation(fields: [festivalId], references: [id], onDelete: Cascade)
  income         Income?
  expense        Expense?
  transfer       AccountTransfer?
  ledgerEntries  LedgerEntry[]

  @@index([mandalId, festivalId, date])
}

model Income {
  id             String          @id @default(uuid())
  transactionId  String          @unique
  festivalId     String
  categoryId     String
  donorId        String?
  donorName      String
  donorPhone     String?
  amount         Decimal         @db.Decimal(12, 2)
  paymentMethod  PaymentMethod
  collectorId    String
  notes          String?
  isAnonymous    Boolean         @default(false)
  createdAt      DateTime        @default(now())

  transaction    Transaction     @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  festival       Festival        @relation(fields: [festivalId], references: [id], onDelete: Cascade)
  category       IncomeCategory  @relation(fields: [categoryId], references: [id])
  donor          Donor?          @relation(fields: [donorId], references: [id])
  collector      User            @relation("IncomeCreatedBy", fields: [collectorId], references: [id])
  receipt        Receipt?

  @@index([festivalId, paymentMethod])
  @@index([collectorId])
}

model Expense {
  id             String          @id @default(uuid())
  transactionId  String          @unique
  festivalId     String
  categoryId     String
  payeeName      String          // Vendor / Payee
  amount         Decimal         @db.Decimal(12, 2)
  paymentMethod  PaymentMethod
  status         ApprovalStatus  @default(PENDING)
  billNumber     String?
  attachmentUrl  String?
  createdById    String
  notes          String?
  createdAt      DateTime        @default(now())

  transaction    Transaction     @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  festival       Festival        @relation(fields: [festivalId], references: [id], onDelete: Cascade)
  category       ExpenseCategory @relation(fields: [categoryId], references: [id])
  createdBy      User            @relation("ExpenseCreatedBy", fields: [createdById], references: [id])
  approvals      Approval[]

  @@index([festivalId, status])
}

model AccountTransfer {
  id             String        @id @default(uuid())
  transactionId  String        @unique
  fromAccountId  String
  toAccountId    String
  amount         Decimal       @db.Decimal(12, 2)
  notes          String?
  createdAt      DateTime      @default(now())

  transaction    Transaction   @relation(fields: [transactionId], references: [id], onDelete: Cascade)
}

model LedgerEntry {
  id            String        @id @default(uuid())
  transactionId String
  accountId     String
  debit         Decimal       @default(0.00) @db.Decimal(12, 2)
  credit        Decimal       @default(0.00) @db.Decimal(12, 2)
  balanceAfter  Decimal       @db.Decimal(12, 2)
  createdAt     DateTime      @default(now())

  transaction   Transaction   @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  account       Account       @relation(fields: [accountId], references: [id], onDelete: Cascade)

  @@index([accountId, createdAt])
}

model Receipt {
  id             String        @id @default(uuid())
  incomeId       String        @unique
  festivalId     String
  receiptNumber  String        @unique // e.g. GNV-2026-000123
  sequenceNumber Int
  shareToken     String        @unique @default(nanoid(12)) // Public secure random access token
  donorName      String
  amount         Decimal       @db.Decimal(12, 2)
  amountInWords  String
  issuedAt       DateTime      @default(now())

  income         Income        @relation(fields: [incomeId], references: [id], onDelete: Cascade)
  festival       Festival      @relation(fields: [festivalId], references: [id], onDelete: Cascade)

  @@index([festivalId, sequenceNumber])
}

model ReceiptTemplate {
  id           String   @id @default(uuid())
  mandalId     String   @unique
  prefix       String   @default("RCP")
  headerText   String?
  footerNote   String?  @default("श्रींच्या चरणी सादर अर्पण. धन्यवाद!")
  signatoryUrl String?
  showQR       Boolean  @default(true)

  mandal       Mandal   @relation(fields: [mandalId], references: [id], onDelete: Cascade)
}

model Approval {
  id         String         @id @default(uuid())
  expenseId  String
  userId     String
  status     ApprovalStatus
  comment    String?
  createdAt  DateTime       @default(now())

  expense    Expense        @relation(fields: [expenseId], references: [id], onDelete: Cascade)
  user       User           @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Area {
  id          String          @id @default(uuid())
  mandalId    String
  name        String          // e.g. Lane 1, Building A-D, Sector 4
  description String?

  mandal      Mandal          @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  households  Household[]
  collectors  AreaCollector[]

  @@index([mandalId])
}

model Household {
  id          String   @id @default(uuid())
  areaId      String
  houseNumber String
  familyName  String
  phone       String?
  targetAmount Decimal @default(0.00) @db.Decimal(10, 2)
  collected   Decimal  @default(0.00) @db.Decimal(10, 2)
  status      String   @default("PENDING") // PENDING, PARTIAL, COLLECTED

  area        Area     @relation(fields: [areaId], references: [id], onDelete: Cascade)

  @@index([areaId])
}

model AreaCollector {
  id      String @id @default(uuid())
  areaId  String
  userId  String

  area    Area   @relation(fields: [areaId], references: [id], onDelete: Cascade)
  user    User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([areaId, userId])
}

model AuditLog {
  id        String   @id @default(uuid())
  mandalId  String
  userId    String?
  action    String   // INCOME_CREATE, EXPENSE_APPROVE, ACCOUNT_TRANSFER, etc.
  entity    String   // Transaction, Member, Settings
  entityId  String
  before    Json?
  after     Json?
  ipAddress String?
  createdAt DateTime @default(now())

  mandal    Mandal   @relation(fields: [mandalId], references: [id], onDelete: Cascade)
  user      User?    @relation(fields: [userId], references: [id], onDelete: SetNull)

  @@index([mandalId, createdAt])
}
